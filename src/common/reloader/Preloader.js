import React from 'react';
import './Preloader.css';

const Preloader = () => {
    return (
        <div className={"preloader"}>
            <h1>LOADING...</h1>
        </div>
    );
};

export default Preloader;
